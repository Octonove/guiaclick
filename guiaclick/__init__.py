"""GuiaClick — crea guias paso a paso grabando lo que haces, 100% en tu PC.

Pulsa grabar, haz el proceso normalmente y GuiaClick captura una imagen en cada
clic, resalta donde pulsaste y redacta el paso. Despues editas, difuminas datos
sensibles y exportas a HTML, Markdown o PDF. Nada se sube a internet: la
alternativa privada a Scribe/Tango para SOPs internos.
"""

from __future__ import annotations

APP_NAME = "GuiaClick"
APP_VERSION = "1.1.4"   # fuente unica de version: build-installer.ps1 la inyecta al .iss
